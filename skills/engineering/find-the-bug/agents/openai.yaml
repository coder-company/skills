interface:
  display_name: "Find the Bug"
  short_description: "Find the cause and prove the fix"
  default_prompt: "Use $find-the-bug to diagnose this failure from evidence and verify the result at the real boundary."
